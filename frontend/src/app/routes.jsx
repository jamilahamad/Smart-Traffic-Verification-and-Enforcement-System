import { PAGES, PAGE_ACCESS, PUBLIC_PAGES, normalizePage, canAccessPage } from '../constants/pages';

export const routes = {
  pages: PAGES,
  access: PAGE_ACCESS,
  publicPages: PUBLIC_PAGES,
};

export { PAGES, PAGE_ACCESS, PUBLIC_PAGES, normalizePage, canAccessPage };

export default routes;